[project]
name = "pss"
version = "1.0.0"
description = "个人记忆搜索引擎"
requires-python = ">=3.10, <3.11"
authors = [
    { name = "iug-cy" }
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"